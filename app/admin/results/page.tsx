"use client";
import { useEffect, useMemo, useState } from "react";
import { AppShell } from "@/components/layout/app-shell";
import { supabase } from "@/lib/supabase";
import { FIXTURES } from "@/lib/fixtures";
export default function AdminResultsPage() {
  const [isAdmin, setIsAdmin] = useState(false); const [scores, setScores] = useState<Record<string, { home: string; away: string }>>({}); const matches = useMemo(() => FIXTURES, []);
  useEffect(() => { async function load() { const { data: authData } = await supabase.auth.getUser(); const user = authData.user; if (!user) return (window.location.href = "/auth/login"); const { data } = await supabase.from("profiles").select("is_admin").eq("id", user.id).single(); if (!data?.is_admin) return; setIsAdmin(true); const { data: matchesData } = await supabase.from("matches").select("id, home_score, away_score"); const map: Record<string, { home: string; away: string }> = {}; (matchesData || []).forEach((m: any) => { map[m.id] = { home: m.home_score?.toString() ?? "", away: m.away_score?.toString() ?? "" }; }); setScores(map); } load(); }, []);
  async function save(matchId: string) { const value = scores[matchId]; await supabase.from("matches").update({ home_score: value.home === "" ? null : Number(value.home), away_score: value.away === "" ? null : Number(value.away), is_finished: value.home !== "" && value.away !== "" }).eq("id", matchId); alert("Rezultatul a fost salvat."); }
  if (!isAdmin) return <AppShell><div className="card p-8 text-lg">Doar adminul poate modifica rezultatele.</div></AppShell>;
  return <AppShell><section className="card p-6 md:p-8"><h2 className="text-3xl font-bold">Admin – rezultate oficiale</h2><p className="mt-2 text-white/70">Introduci scorurile reale, iar clasamentul se recalculează automat.</p></section><div className="mt-6 grid gap-4">{matches.map((match: any) => <div key={match.id} className="card p-4"><div className="font-semibold">{match.home_team} vs {match.away_team}</div><div className="mt-3 flex flex-wrap items-center gap-3"><input className="input max-w-[90px] text-center" type="number" min="0" value={scores[match.id]?.home ?? ""} onChange={(e) => setScores((prev) => ({ ...prev, [match.id]: { home: e.target.value, away: prev[match.id]?.away ?? "" } }))} /><span>-</span><input className="input max-w-[90px] text-center" type="number" min="0" value={scores[match.id]?.away ?? ""} onChange={(e) => setScores((prev) => ({ ...prev, [match.id]: { home: prev[match.id]?.home ?? "", away: e.target.value } }))} /><button className="btn-primary" onClick={() => save(match.id)}>Salvează rezultat</button></div></div>)}</div></AppShell>;
}