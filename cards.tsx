import { formatKickoff } from "@/lib/scoring";
import { Match } from "@/lib/types";
export function StatCard({ label, value }: { label: string; value: string | number }) { return <div className="card p-4"><div className="text-sm text-white/60">{label}</div><div className="mt-2 text-3xl font-bold text-fifa-gold">{value}</div></div>; }
export function MatchCard({ match, children }: { match: Match; children?: React.ReactNode }) { return <div className="card p-4"><div className="mb-2 flex flex-wrap gap-2 text-xs uppercase tracking-wide text-white/60"><span>{match.stage === "groups" ? `Grupa ${match.group_name}` : match.stage}</span>{match.matchday ? <span>Etapa {match.matchday}</span> : null}</div><div className="text-lg font-semibold">{match.home_team} <span className="text-white/40">vs</span> {match.away_team}</div><div className="mt-2 text-sm text-white/70">{formatKickoff(match.kickoff_at)} · {match.venue ?? "-"}</div><div className="mt-1 text-sm text-white/50">Deadline: {formatKickoff(match.lock_at)}</div>{children ? <div className="mt-4">{children}</div> : null}</div>; }